export class Result {

    constructor(public player: string, public opponent: string, public gameType: string) {
        this.player = player;
        this.opponent = opponent;
        this.gameType = gameType;
    }
}
